<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Dark rounded tile with the glowing accent dot from the header -->
  <defs>
    <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="6"/>
    </filter>
  </defs>
  <rect width="64" height="64" rx="14" fill="#0a0b0e"/>
  <circle cx="32" cy="32" r="13" fill="#6aa5ff" opacity="0.65" filter="url(#glow)"/>
  <circle cx="32" cy="32" r="9" fill="#6aa5ff"/>
</svg>
